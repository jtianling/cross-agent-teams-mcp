# Changelog

## Unreleased

### Fixed

- **key 矛盾的 pre-reg 行不再被别人绑走并消费 (联测现场事故).**  实测现象: 一个自身 pre-reg 行已过期、又没有任何 same-thread 证据的 codex (托管在 %70) 合法进入 pre-reg 扫描, 而扫描此时唯一的关联手段是 "全机唯一候选 + 该 pane 的 tty 上跑着 argv 带此行 uuid 的前台 codex" —— uuid 证明的是**那个 pane 里的 codex 身份**, 从不证明**调用者身份**.  于是它绑走并消费了 %71 (另一把 key 的 shell codex) 的行: key 因 caller 已持不同 key 未被附上 (旧语义只挡到这一层), 但 shell 既丢了自己的 pre-reg 行也丢了 pane, 最终无绑定无 key, 而抢走的一方座位指向了别人的 pane (poke 会打进别人的窗口).  修复: 行内 key 判定为"别人的"时, 该行**整行不作候选** —— 不绑 pane、不消费行、不附 key, 行留给真正的主人, 没有候选时按既有 fail-closed 落到 `detect_tmux_pane` fallback.  两种判定各记 debug reason (决不含 key 值): caller 行已持不同 key → `identity_key_contradiction`; key 的 holder 是另一个 (team, name) 且进程存活 → `identity_key_live_holder_conflict` (这条把 **caller 自己无 key** 的形态也覆盖了, 例如存量未播种的 codex 重启时抢别人的行).  判定提前到候选资格, 而不是等绑完再只挡 key.  caller 无 key 且 key 无 holder (首次播种)、key 相同 (幂等)、holder 已死 (重启恢复)、行内无 key 四种正路行为不变; caller 行惰性读取, 行内无 key 时完全不触碰 attach 依赖.  隔离实验室 (`lab/`) 复现该事故时又补出两条: ① holder 是另一身份但没有正数 pid 时, 存活性是 UNKNOWN 而非 dead (tty/pane 绑定本就不记 pid), 候选资格同样剔除 (`identity_key_holder_liveness_unknown`), 与 seat-follow 的 liveness 教训同源; ② **`detect_tmux_pane` fallback 不再绑定仍挂着未过期 pre-reg 行的 pane** (`pane_has_pending_prereg`) —— 行还在说明某 launcher announce 了那个 pane 而它的 codex 尚未注册, caller 若真是它, 扫描早就凭 uuid + 前台载体证明消费了; fallback 是全机打分、零调用者关联, 让它绑这种 pane 等于用启发式重建扫描刚用证据拒绝的认领 (实验室第一次跑正是这样漏过去的: 扫描拒绝了, fallback 又绑上了同一个 pane)。 R19 评审又打回两条并已修: ③ **候选资格不再复用 `planIdentityKeyBinding`** —— 那条规则是在 caller 已证明拥有 pane 之后仲裁 key 的, 会排除针对 caller 自己 ui_pid 的冲突; 而扫描阶段手上只有**候选 pane 的载体 pid**, 传进去恰好在"存活的外来 holder 就是那个 pane 的前台 codex"(holder pid === 候选 pid) 这一最真实形态下自我豁免, 等于在最该拦的地方开门.  候选资格改为只认正面证据: 另一身份的 key 一律 foreign, 除非该身份可证已消失 (有正数 pid 且探测为不在运行).  ④ **仲裁与最终提交共享提交前提**: 候选判定在 bind 的异步验证之前, 真正的 owner 可能在窗口内拿到 key; 现在 bind 之后于同一同步事务内重新仲裁并条件消费, 判定 foreign 时按本次注册 generation 条件回滚已写入的绑定, 不消费不附 key (只在消费后拒绝附 key 不满足契约 —— 那时行已删、座位还指着别人的 pane).  R20 评审指出"先绑再回滚"仍不成立并已结构性修正: runtime 写会按 LWW 清空**在位 agent** 的 pane 绑定, 事后只清 caller 行无法把它还回去 —— 真正主人的投递路由已被破坏.  现在 bind 拆成**异步验证**(只探测不落库)与**同步提交**, "重新仲裁 + runtime 写 (含在位驱逐) + 条件消费 + 附 key" 整体进同一事务: 任何拒绝或抛错都整体回滚, 绑定不是事后撤销而是根本没落地; 附 key 失败连带回滚消费 (消费掉却没附上 key 等于永久毁掉恢复句柄); 事务依赖改必填, 不留无事务降级模式.  detect fallback 的 "目标 pane 无有效 pre-reg 行" 检查同样压进它最终写的事务 (两种 fallback 形态在检查后都还有 await, 窗口内新 announce 的 pane 否则会被零关联的 bind 覆盖).  R21 评审再补上最后一个洞: 上述"附 key 失败连带回滚"此前**只对抛出的异常成立**, 而附 key 内部还有三条**返回式拒绝** (caller 行不存在 / caller 持另一把 key / planner 判定 holder 在线) 是 log + return 的, 事务照常提交 —— 提交下去的正是本条要防的最坏状态: 在位 pane 已被 LWW 驱逐、恢复行已消费、key 却没附上, 而那行是这把 key 的唯一载体.  现在拒绝一律转成抛错, 与异常走同一条回滚路径 (`identity_key attach refused: reason=...` 随 `auto-bind commit rolled back` 记录, 已 redact key 值); 教训与本条同源 —— **"拒绝"和"回滚"不是一回事, 只要拒绝是 return 出来的, 事务就不知道它发生过**.  已知残余: caller 无 key 且行内 key 也无 holder 时 daemon 手上没有任何证据可用, 该行仍可能被另一个 codex 认领 —— 根因是 codex `--remote` 调用方无法自证 pane, 根治需要注册时能带上自己的 `xats.agent_id` (跨仓库契约, 另立 change).  该残余已由真实 codex 在隔离实验室复现 (2026-07-31), 同一次跑还测出: **真实 codex 注册时并不传 identity_key** (尽管环境变量在进程里、MCP instructions 也明确要求), 因此 `identity_key_contradiction` 只对"agents 行上已存过 key"的身份有效 (行上的 key 靠 `COALESCE(excluded.identity_key, identity_key)` 在无 key 重注册时保留), 首次注册的身份落不到这条最强证据上 —— 任何依赖调用方自报身份的规则都不可靠。

- **codex 同 thread 重注册决不扫外来 pre-reg 行、决不全局探测 (same-thread 证据统一语义).**  线上实测事故: 会话 A (`aoe-codex`, codex-appserver thread T, 已绑 pane/pid, 持 identity_key K1) 同 thread rename 成 `aoe-codex-r2` 时, 一个无关 shell codex 的 pre-reg 行恰好 pending; auto-bind 唯一的关联是 "全机唯一候选", 与调用者零关联, rename 注册于是消费了外来行 — r2 绑走 shell 的 pane/pid 并附上 shell 的座位 key, shell 自己的注册扫不到行, 且 r2 座位指向错误 tty 导致 seat-follow 找不到 K1 holder, K1 永不迁移.  修复 (统一语义): codex 注册在扫 pre-reg 之前先解析 same-thread 会话证据 — 同 device 上 thread 相等且仍有 runtime 绑定 (`runtime_ui_pid` / `runtime_tty`) 的行 (`AgentsRepo.findRuntimeByThread`, 返回完整座位字段); caller 自己被 upsert 复用的行也算证据, 条件是 upsert 前存储的 thread 等于本次注册 thread (register 流程在写入前捕获 pre-upsert thread — upsert 保留 runtime 但覆盖 thread, 同名同 thread 重注册因此不再漏到外来扫描).  只要存在任何证据, 本次注册就决不扫 pre-reg、决不跑全局 `detect_tmux_pane` (两者都无 caller 关联, 走到就可能拿走外来 pane/pid/座位 key).  证据行按物理座位折叠 (rename 链 A→B→C 天然留多行 — 旧行 pid/tty 原样、只有 pane 被 last-writer-wins 清空; 共享正数 pid / tty 归并为一个座位, last-writer-wins 持有者胜出), 唯一座位精确继承: 正数 pid 走 `bind_runtime_identity` pid 路径 (fresh 校验 pid→tty→pane 存活), pid-less 用记录的 tty/pane 精确绑定 (不探测); 多个不同物理座位、继承 bind 失败、或无可绑定信息一律 fail-closed — 不扫 pre-reg、不全局探测、不绑 runtime, register_agent 照常成功走标准 no-pane hint 路径.  继承成功后既有 seat-follow 钩子照常按 thread 等同迁移 K1.  只有无任何证据的注册 (真正的新 thread, 如重启恢复) 才走 pre-reg 扫描与 detect fallback — 重启恢复流不受影响.  另外, pre-upsert 捕获与持久化之间隔着异步 app-server 探测, 并发同名注册可在该窗口内改写行, 使早期捕获双向失真 (漏掉真实 caller-row 证据落到外来扫描, 或把并发会话刚写入的座位当成自己的继承证据): register upsert 事务现在原子返回行的实际 prior 状态 (prior thread + 完整座位字段, 同一事务内 SELECT prior → upsert), tool 层做 CAS 校验 — 不等即本次注册 runtime auto-bind fail-closed (无 caller-row 证据、不扫 pre-reg、不探测、不绑 runtime, register 照常成功, cas_drift 独立日志 reason), 相等时以事务返回的 prior thread 为准.  same-thread 全部决策 (none / 继承成功与失败 / ambiguous / cas_drift) 经单一决策点记 debug 日志, 各带行数、座位数与涉及 agent id, 决不含 key 值.  隔离实验室的端到端恢复场景 (S7) 随后测出这条语义**掐死了恢复本身**: 座位记录的 pid 属于已死载体时继承不可能, 而"继承失败一律 fail-closed"让重启后的 codex 被 recovery poke 叫醒却无处落地 — pre-reg 行无人消费、key 附不上, 两种重启形态 (in-pane 保 pty / `respawn-pane -k` 换 pty) 红在同一条 reason 上, 且上游 recovery 链路 (调度/探测/投递) 全绿并已解析出新 carrier.  语义因此改为**证据"指向"而非"终结"**: 座位载体**确证已死** (记录了正数 pid 且探测为不在运行) 时继续走 pre-reg 扫描, **且只到扫描** — 全局 `detect_tmux_pane` 仍然够不着, 因为座位没了并不说明 caller 现在在哪个 pane; 扫描本身不是猜测 (要求 launcher announce 的 pane + 含存储 uuid 的前台载体证明 + 非外来的 key 归属).  两条护栏: ① 无正数 pid 的座位 (tty/pane 绑定本就不记 pid) 是存活性 UNKNOWN 而非已死, 仍 fail-closed, 与 identity_key 仲裁、seat-follow 同源; ② bind 因**注册已被取代** (`stale_registration_bind`) 而失败时仍然终止, 与存活无关 — 行已推进过的注册不得据此获得任何新路径, 把这条交给扫描自身的 generation 条件写去挡是纵深防御而非规则本身 (该护栏由既有并发用例抓出: 第一版修复只判存活, 会把被取代的注册送进扫描并在决策日志里贴错标签).  新 outcome `inherit_seat_vacated`, 日志尾巴写明只到扫描.
- **register-time runtime bind 的迟到写不再踩掉更新的同名注册 (register_generation 条件写).**  CAS 只封住探测窗口, 封不住 bind 窗口: CAS 通过后, register-time runtime bind 仍要 await 异步验证 (pid/tty/pane 探测), 而最终写此前只按 `agent_id` 无条件 UPDATE; 同 `(device, team, name)` 重注册复用同一 agent_id, 于是注册 A 挂起在 bind 验证中、并发注册 B 完成同名注册 (thread U + 座位 S2) 后, A 的迟到写会把行踩成 `thread U + 座位 S1` 的跨会话杂交 (takeover 的 transport close 不会取消 A 已在运行的 handler).  修复: agents 表加 `register_generation` (INTEGER NOT NULL DEFAULT 0, 幂等迁移), register upsert 同一事务内自增并把铸出的 generation 随注册结果内部线程化到 tool 层 (对外 envelope 与 prior_snapshot 一并剥离); 全部四条 register-time bind 路径 (显式 ui_pid、same-thread 继承、pre-reg auto-bind 消费、detect fallback) 把本次注册的 generation 传到 setRuntimeBinding, 最终写变为条件写 `WHERE agent_id = ? AND register_generation = ?` — 0 行即该次 bind fail-closed: 不写任何 runtime 字段、跳过在位 pane 驱逐、不跑 seat-follow, 记 `stale_registration_bind` 独立 reason 日志 (只记数量与 agent id, 决不含 key 值), register_agent 照常成功.  用户显式调用的 `bind_runtime_identity` tool 没有自己铸出的 generation, 但同类竞态照样存在 — service 现在在调用开始捕获 caller 行当前 generation 作条件写期望 (capture-at-call-start): 调用前已完成的历史注册不挡显式修复, 调用期间落地的同身份新注册使条件写 0 行、同样 `stale_registration_bind` fail-closed; 经 BindRuntimeIdentityService 的每一次最终写都是条件写.  注册成功结果的 `register_generation` 与 `prior_snapshot` 在类型上必填 (prior 为 null 仅表示新行合法无 prior), tool 层运行时校验 generation 为正 safe integer (NaN/Infinity/负数会让条件写静默 0 行)、codex 结果缺 prior 字段按 CAS drift 处理, 均记 invariant error 并让 runtime auto-bind fail-closed; register-time 入口 (tools.ts 四条路径 helper 与 AutoBindCodexPaneInput) 的 generation 参数类型必填; service 边界的 generation mode 为 discriminated 二选一 (`expectedRegisterGeneration: number` 或 `captureCurrentGeneration: true`), 手动 rebind tool 显式选 capture, 任何调用方都无法靠漏传落入 capture 语义; invalid generation + CAS drift 的注册响应带专用 invariant hint (残留 pane 绑定可能仍在), 不复用会谎称无 pane 的标准 no-pane hint — 条件写决不静默退化为无条件或 no-op 写.
- **CAS drift 注册不再残留被竞争会话的座位 (条件清空).**  drift fail-closed 此前只"不再新绑", 但 register upsert 用 COALESCE 保留了被竞争行的座位字段, 行会终结为 "本次注册的 thread + 被竞争会话的座位" 杂交, 而 codex poke 在 appserver 普通失败时会 fallback 到该 pane (误投递到别人的终端).  修复: drift 检出后用本次注册铸出的 generation 条件清空全部 runtime 座位字段 (`tmux_pane_id` / `runtime_ui_pid` / `runtime_tty` / `runtime_verification_mode` / `runtime_bound_at`, `AgentsRepo.clearRuntimeBinding`); 更新一代注册已推进 generation 时清空 0 行、其新绑座位不被触碰; 清空结果随 cas_drift 决策一起记 debug 日志.  行终态是真正的 unbound: appserver 投递 (按 thread) 不受影响, tmux fallback 无 pane 可误投.
- **Orphan-session GC default grace lowered to 5 min (from 30 min).**  After 0.5.12 raised the grace to 30 min, a long-running daemon under multi-client load OOM'd (V8 "Ineffective mark-compacts near heap limit") after ~73 min, because misbehaving clients that connect-and-idle in a loop could accumulate orphan-session state for the full 30-min window.  5 min keeps the human-paced register_agent workflow protected (combined with the loopback companion that lets local clients skip the device-spoofing check entirely) while bounding worst-case orphan accumulation 6x tighter.  Override via `ORPHAN_GC_IDLE_MS` env var or `opts.orphanGcIdleMs`.
- **未注册 MCP session 增加硬上限.**  Orphan GC 现在除了 idle 窗口外, 还会按 `ORPHAN_GC_MAX_AGE_MS` 强制关闭长期未注册但持续有心跳的 session, 并用 `ORPHAN_GC_MAX_SESSIONS` 限制同时保留的未注册 session 数量。  已完成 `register_agent` 的 session 仍然永不被该 GC 按时间或数量断开。
- **未注册 MCP session 默认上限调整为 500.**  `ORPHAN_GC_MAX_SESSIONS` 的默认值从 100 调整为 500, 让上限更像异常保险丝, 避免正常全局 MCP 配置连接峰值过早触发 cap。
- **channel proxy 注册失败不再泄漏未注册 MCP session.**  当 daemon 拒绝 proxy 的 `register_agent` 或 `subscribe_channel_wake` 调用时, proxy 现在会先发送 Streamable HTTP `DELETE` 终止 session, 再关闭本地 client/transport。  失败重试会走低频退避, 避免远端全局 MCP 配置在注册失败时高频创建新 sid。
- **channel proxy 默认失败重试改为固定低频档位.**  proxy 注册失败后默认按 `1s -> 10s -> 60s -> 600s` 重试, 后续保持 10 分钟间隔。  远端全局 MCP 配置注册失败时仍会自恢复, 但不会持续制造高频连接 churn。
- **Orphan session reap 日志默认静默.**  daemon 不再默认把 `mcp orphan session reap ...` debug 行写到 console, 避免后台 `&` 启动时污染当前 shell。  测试或嵌入式调用仍可显式传入 MCP logger 观察这些事件。

### Added

- **codex pre-register 携带 identity_key 与 daemon 端恢复 poke.**  `codex_pane_pre_registrations` 表新增可空 `identity_key` 列 (幂等迁移, 与 `xats_agent_id` 分列存储); `pre_register_codex_pane` tool 加可选 `identity_key` 参数, `pre-register-codex-pane` CLI 加可选 `--identity-key-env [VAR]` 标志 (默认读 `XATS_IDENTITY_KEY`, key 从环境变量取值, 决不出现在任何 argv 上; 标志存在但环境变量缺失/为空时本地 fail-fast 退出 2, 不触达 daemon), key 只走 CLI/HTTP 通道, 决不进 codex argv.  带 key 的 pre-register 命中已知身份且 holder 进程已死时, daemon 调度一次恢复 poke: 探测到该 pane 的 tty 上出现 argv 含对应 uuid 的 codex `--remote` 进程后才首发 (复用 auto-bind 探测原语, 5s 轮询, 行过期/被消费/被覆盖即终止), 发送走 quiet-guard + pane-host-verify + 复合同步 ownership 确认 (行快照/holder 元组/目标侧前台载体证明/未取消), 由共享 tmux poke 原语在 pre-capture、paste 前与 Enter 前三个写检查点各执行一次 — 前台载体证明用同步 `ps -t <tty> -o pid=,pgid=,tpgid=,stat=,command=` 要求 codex pid 的 STAT 无 T/t/Z、命令仍为含存储 uuid 的 codex `--remote`、且 pgid === tpgid (tty 前台进程组; 单纯 kill(pid, 0) 存活不算数, SIGSTOP/后台化的 codex 会被拒绝), 任何探测失败按不安全处理; Enter 前失败以新错误 `ownership_lost` 中止, 已 paste 的文本不执行 (该 pre-Enter 检查点对所有 poke 路径生效); 瞬态拒绝 (`guard_failed` / 载体后台化且未写入) 不入长退避梯子, 同代回到 5s 探测轮询续试, 以行生命周期 (过期/覆盖/消费) 为界; 各代以 generation token (`codex-recovery:<pane_id>:<generation>`) 隔离, 过期闭包只能取消自己那一代; 终局取消 (消费→row_consumed / 覆盖→row_replaced / 关停→daemon_shutdown / 过期→row_expired) 各记一条带 ISO 时间戳的取消日志 (决不含 key 值); daemon 关停在 db 关闭前清空全部恢复调度与各代 token.  同一前台载体证明同时覆盖: auto-bind 候选过滤 (后台 codex — STAT S 但 pgid !== tpgid、shell 在前台 — 不绑定、不消费 pre-reg 行) 与普通 codex poke 的 tmux fallback (目标绑定了 runtime_ui_pid 时三个写检查点同样要求该 pid 是 pane tty 的前台 codex `--remote`, 此路径无存储 uuid 用命令级匹配, pane tty 同步解析, 探测失败 fail-closed; 非 codex 目标保持原 DB ownership 确认不变).  tmux poke 原语在 load-buffer 成功后的任何中止路径上 best-effort `delete-buffer`, 消息体不再滞留在 named buffer 里.  poke 话术引导 codex 以恢复出的 (team, name) 调 `register_agent` (thread_id 用 `$CODEX_THREAD_ID`), 话术不含 key.  auto-bind 消费带 key 的行时按 `planIdentityKeyBinding` 四分支把 key 附到 caller 行, 行内 key 与 caller 行已持非空 key 矛盾时该行整行不作候选 (见下条), 附 key 失败不腐蚀 register_agent 结果; 消费即取消 pending 恢复调度; bind 期间行被覆盖 (stale outcome) 时不消费新行、不附 key、不取消新行调度、也不跑 seat-follow.  **与 aoe (agent-of-empires) 的配对契约**: aoe bootstrap 侧改传 `--identity-key-env` (launcher 已在 pane shell 中导出 `XATS_IDENTITY_KEY`, key 值不落 argv), 为可选标志, 带标志调用失败时降级为不带标志重试 (老版本 daemon 对未知参数返回 invalid_arguments 即触发降级); codex 冷启动偏慢时由 aoe 侧传 `ttl_seconds` 放宽 (daemon 默认 120s / 上限 600s 不变); 发布顺序 xats 先发版.  不带 identity_key 时所有现有 pre-register / auto-bind / poke 路径行为与现状完全一致.
- **同 pane 重注册 (rename) 时 identity_key 跟随座位迁移 (seat-follow), 存活 holder 只认 codex thread 等同.**  codex 注册的 runtime 绑定落定后 (pre-reg auto-bind 与 `detect_tmux_pane` fallback 两条路径), daemon 按 rebind 后仍存活的字段 (`runtime_ui_pid` / `runtime_tty` — 旧行的 `tmux_pane_id` 已被 last-writer-wins 清空) 查找同 device 上仍持 key 的其他行.  恰好一个候选时: **存活 holder 只在 caller 行与 holder 行的 codex-appserver `thread_id` 相等时迁移** (同会话 rename 天然带着 holder 行已有的同一 thread — 可验证的 caller↔process 关联); fallback 绑定虽会用前台载体探测把真实 codex pid 记到 caller 行 (`verified_pid_tty_pane`, 供存活判定与 poke 载体确认用), 但该 pid 来自全局 pane 启发式、与注册调用者无关联, pid/tty 相等对存活 holder 永不足够, thread 任一侧缺失或不等一律 fail-closed 不动只记 debug; holder 行没有记录 pid (或 pid 非正数) 时存活性为 UNKNOWN, 决不当作死亡 (pid-less 是合法存活状态 — verified_tty_pane 绑定本就不带 pid), 与存活 holder 同等对待仍只认 thread 等同, 拒绝时 debug 记 liveness_unknown 与 thread 原因; 死 holder 分支只对正数记录 pid 且仲裁时刻 fresh 存活复查确认已不在运行的 holder 开放, 同座重启保持既有 `planIdentityKeyBinding` 四分支 migrate (旧行清 key 与新行绑 key 同一事务); caller 已持 key (seeding 附 key 已跑) 幂等跳过; 0/多候选 no-op 只记候选数量.  另外 carrier pid 绑定失败 (pid_not_found / pid_pane_tty_mismatch) 不降级 tty-only 绑定: 只此一次尝试, 不跑 seat-follow, key 不动.  任一失败走 redacted 结构化日志 (决不含 key 值), 决不腐蚀 register_agent 结果.  迁移后 `findByIdentityKey` 解析到新 (team, name), 后续 pane 重启的恢复 poke 指向新名字而非旧名字, 恢复模块零改动.
- **Loopback companion listener.**  When the daemon binds to a non-loopback-covering host (e.g. a LAN IP like `192.168.1.102`), `startServer` now also binds a second HTTP listener on `127.0.0.1` at the same port that reuses the primary Fastify request handler.  This lets same-host clients connect via `http://127.0.0.1:<port>/mcp` and be classified as `local` origin (auto-filling the daemon's local device label, bypassing the remote spoofing check), while LAN peers still connect via the primary host.  Avoids the alternative of `--host 0.0.0.0`, which would also expose the daemon on every other interface (VPN, public IPs).  Skipped automatically when the primary host already covers `127.0.0.1` (`127.0.0.1`, `localhost`, `0.0.0.0`).  Opt out with `--no-loopback-companion` / `loopbackCompanion: false`.  Companion bind failure is fatal — if a local client config relies on `127.0.0.1:<port>`, silently starting only the LAN listener would leave it broken.
- **`/health` exposes MCP session metrics.**  The health response now includes `mcp_sessions.total`, `registered`, `orphan`, and `fanout`, so daemon-side session growth can be observed without heap snapshots.

### BREAKING

- Removed 9 MCP tools: `register_contract`, `subscribe_contract`, `get_contract`, `diff_contracts`, `pending_contract_events`, `task_add`, `task_claim`, `task_complete`, and `task_list`.
- Removed the unused task/contract storage surface.  On startup, legacy SQLite tables `tasks`, `contracts`, and `contract_subscriptions` are dropped with `DROP TABLE IF EXISTS`.
- `unregister_self` no longer checks for in-progress tasks and no longer returns the `tasks_in_progress` error branch.

### Fixed

- **Orphan-session GC is now idle-based, not age-based.**  The reaper in `src/mcp/transport.ts` previously closed any pre-registration session older than 60 s, which evicted Claude Code's HTTP MCP session before a human-paced `register_agent` could land and returned `unknown_session` on the call.  The reaper now tracks `lastActivityAt` per session (bumped on every POST/GET/DELETE that matches an existing session) and closes only sessions whose idle time exceeds `graceMs`.  Default grace raised to 30 min, overridable via `opts.orphanGcIdleMs` / env `ORPHAN_GC_IDLE_MS`.  Crashed clients (no traffic at all) are still reaped; clients that send any request inside the window stay alive.
- **Daemon SIGTERM no longer hangs when long-lived clients are attached.**  The graceful shutdown handler in `src/daemon/shutdown.ts` now bounds `app.close()` with a deadline (default `5000 ms`, overridable via `XATS_SHUTDOWN_GRACE_MS`).  When the deadline expires, the handler calls `app.server.closeAllConnections()` to terminate stragglers (typically channel-proxy SSE streams and Claude Code MCP subscription streams), releases the pid file, and exits `0`.  Previously the daemon could be left in a half-dead state (listener gone, process alive) until `kill -9`.
- **Second SIGTERM / SIGINT is now a fast exit.**  Receiving a repeat signal during drain skips the remaining wait, still releases the pid file, and exits `0` within ~200 ms.  Signal handlers moved from `process.once` to `process.on` with an internal `shuttingDown` flag so repeat signals are observed instead of falling through to Node's default terminator (which previously skipped pid-file cleanup and exited `143`/`130`).

## 0.5.1

### Fixed

- **Daemon session leak (OOM in ~20 min under multi-host load).**  Three layered fixes target the feedback loop where channel-proxy reconnects accumulated phantom MCP sessions on the daemon side:
  - `register_agent` cross-session re-claim of an existing `(team, name)` is now a TAKEOVER instead of returning `{ error: 'agent_id_collision' }`.  The daemon force-closes the prior MCP transport, releases the binding, and accepts the new registration.  The within-session Authorization-mismatch HTTP 409 path is preserved.  Existing in-memory bindings move to the new session id; SSE fanout, channel-wake fanout, and Authorization-hash bindings are detached via the existing `transport.onclose` chain.
  - `mountMcp` adds an orphan-session GC that force-closes any session whose `agentIdHolder.current === undefined` and `Date.now() - createdAt >= 60_000`.  Sessions that completed `register_agent` are NEVER touched.  The ticker runs in `buildServer` next to the existing cleanup ticker; default tick 60 s, configurable via `opts.orphanGcIntervalMs` and env `ORPHAN_GC_INTERVAL_MS`.
  - Channel proxy `waitForDisconnect` default heartbeat raised from 200 ms to 30 000 ms.  `transport.onclose` remains the primary disconnect signal; echo polling is now a coarse-grained backstop.  Override stays available via `ReconnectingProxyConfig.healthCheckIntervalMs` for tests.

### Migration

- API consumers that previously relied on `agent_id_collision` as a guard against double-registration MUST treat the second `register_agent` from a different MCP session as a takeover that returns the (existing) `agent_id` and assume the prior session is closed.

## 0.5.0

### BREAKING

- Renamed the `register_agent` input field `client` → `agent_type` and `client_name` → `agent_type_name`.  The Zod schema strictly rejects the legacy keys with a rename hint (`Unrecognized key in register_agent input. Note: the fields ``client`` and ``client_name`` were renamed to ``agent_type`` and ``agent_type_name`` in 0.5.0.`).  Migration: rename the field name at every call site.  The string enum values (`'codex' | 'claude-code' | 'opencode' | 'custom'`) are unchanged.
- Renamed the `agents` table columns: `agents.client` → `agents.agent_type`, `agents.client_name` → `agents.agent_type_name`.  An idempotent startup migration uses `ALTER TABLE agents RENAME COLUMN` to migrate legacy databases in place; existing data is preserved.  Fresh databases are created directly with the new column names.
- Renamed the exported TypeScript type `ClientKind` → `AgentType` (file moved from `src/lib/client-kind.ts` to `src/lib/agent-type.ts`).  The string union members are unchanged.
- Renamed the `list_agents` response keys `client` / `client_name` → `agent_type` / `agent_type_name` to mirror the input rename.  Other response keys are unchanged.
- Updated the `register_agent` tool description, MCP `serverInfo.instructions` string, and the bind-channel/pre-register-codex-pane tool descriptions to use `agent_type=` everywhere `client=` previously appeared.  The error envelope code stays `ui_pid_client_mismatch` for backwards-compatibility with existing handlers, but its detail string now reads `agent_type="..."`.
- The MCP tool field name on `detect_tmux_pane({agent})` and `bind_runtime_identity({agent})` STAYS `agent` — only the internal TypeScript type alias renames to `AgentType`.  No wire-format change for those two tools.
- Bumped to `0.5.0` (after `0.4.0`'s collapse-register-self-tools); under 0.x a minor bump signals a breaking change in this codebase's npm publish line.

### Internal

- `AgentsRepo.setClient(...)` renamed to `AgentsRepo.setAgentType(...)`.
- `RegisterInput` and `AgentRow` interfaces in `src/storage/agents-repo.ts` and `src/mcp/register-agent.ts` use `agent_type` / `agent_type_name` instead of `client` / `client_name`.
- All affected tests (~40 files referencing the field on the MCP tool surface or the SQLite column) were updated mechanically.  A new test file `tests/rename-client-to-agent-type-migration.test.ts` covers the column-rename startup migration (legacy schema → renamed, idempotency on already-renamed schema, fresh database).  Two new test cases in `tests/register-agent-tool-schema.test.ts` cover the legacy-key rejection paths.

## 0.4.0

### BREAKING

- Removed the `register_claude_self` MCP tool.  Migration: call `register_agent({ client: "claude-code", ... })` with the same arguments.  All behaviors (model auto-default via session client info, channel auto-bind via `ui_pid`, csid-vs-ui_pid consistency check) are preserved on the unified entry point.
- Removed the `register_codex_self` MCP tool.  Migration: call `register_agent({ client: "codex", thread_id, ... })`.  `thread_id` is now REQUIRED at the schema layer when `client="codex"`; missing or empty `thread_id` is rejected by Zod before any handshake runs.  The previous `thread_id_required` candidate-list envelope is no longer returned on the unified surface — launcher pre-reg callers should use `pre_register_codex_pane`.
- The `register_agent` tool description now contains a top-level DETECTION block instructing LLM callers to mechanically determine `client=` via shell probes — only `CODEX_THREAD_ID` (codex) and `CLAUDECODE` / `CLAUDE_CODE_ENTRYPOINT` (claude-code) are promoted as active probes; everything else falls through to `client="custom"` + `client_name`.  The previously promoted `command -v opencode` probe was removed because it detects what the user installed, not what runtime the LLM is inside (in field testing it caused cursor to misregister as opencode).  `CURSOR_TRACE_ID` is mentioned only as an in-line example for choosing `client_name="cursor"` under the custom fallback, not as a separate probe.
- The MCP `serverInfo.instructions` string was rewritten to describe `register_agent` only and includes the same anti-pattern warning ("do NOT guess from system-wide signals like 'binary X is on PATH'").
- `model` is now OPTIONAL for any `client` kind.  The previous schema rejection of `model === undefined` for non-claude / non-codex clients was removed; `agents.model` stores NULL when omitted.  Pass `model` only when you have an authoritative identifier you want surfaced via `list_agents`.

### Internal

- `RegisterCodexSelfService` continues to back the codex-appserver registration path inside `executeRegister` for `register_agent({ client: "codex", thread_id, ... })`.  Only the MCP-tool wrapper was removed.
- `defaultClaudeSelfModel` and the codex `ws_url=""` default were inlined into `executeRegister`, gated on `client`.
- `RegisterInput` (in both `src/mcp/register-agent.ts` and `src/storage/agents-repo.ts`) widened `model: string` to `model?: string` so the optional path can flow through to the SQLite INSERT (which binds NULL for an undefined value).
